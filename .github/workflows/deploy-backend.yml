name: Deploy Backend

on:
  workflow_run:
    workflows: ['Backend Tests']
    types: [completed]
    branches: [master]
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment'
        type: choice
        default: 'staging'
        options: [staging, production, seo]

concurrency:
  group: deploy-backend-${{ github.event.inputs.environment || 'staging' }}
  cancel-in-progress: false

jobs:
  build:
    name: Build image
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    permissions: { contents: read, packages: write }
    outputs:
      tags: ${{ steps.meta.outputs.tags }}

    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/backend
          tags: |
            type=sha,prefix=
            type=raw,value=latest

      - uses: docker/build-push-action@v5
        with:
          context: .
          file: ./backend/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}

      - run: echo "✓ Pushed ${{ steps.meta.outputs.tags }}"

  deploy-staging:
    name: Deploy → Staging
    needs: build
    if: github.event_name == 'workflow_run' || (github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'staging')
    runs-on: ubuntu-latest
    environment: staging

    steps:
      - run: npm install -g @railway/cli
      - name: Deploy
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN_STAGING }}
        run: |
          echo "🚀 Deploying backend to staging..."
          railway redeploy --service node-core --yes
          echo "✓ Deploy triggered"

  deploy-production:
    name: Deploy → Production
    needs: build
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'production'
    runs-on: ubuntu-latest
    environment: production

    steps:
      - run: npm install -g @railway/cli
      - name: Deploy
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          echo "🚀 Deploying backend to production..."
          railway redeploy --service node-core --yes
          echo "✓ Deploy triggered"

  deploy-seo:
    name: Deploy → SEO
    needs: build
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.environment == 'seo'
    runs-on: ubuntu-latest
    environment: seo

    steps:
      - run: npm install -g @railway/cli
      - name: Deploy
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN_SEO }}
        run: |
          echo "🚀 Deploying backend to SEO..."
          railway redeploy --service node-core --yes
          echo "✓ Deploy triggered"
